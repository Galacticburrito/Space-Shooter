[package]
name = "shooter"
version = "0.1.0"
edition = "2024"

# enable high optimizations for dependencies but not code
[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 1

[dependencies]
bevy= "0.15"
bevy-inspector-egui = "0.30.0"
rand = "0.9.0"
